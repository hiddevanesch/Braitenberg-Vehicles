package nl.group5b.model.interfaces;

import nl.group5b.engine.Renderer;

public interface DriveHandler {
    // This function should "move" a body based on body behaviour
    void move(Renderer renderer);
}
